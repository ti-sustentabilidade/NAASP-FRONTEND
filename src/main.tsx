import React from "react"
import ReactDOM from "react-dom/client"
import "./index.css"
import LoginIndex from "./pages/login-page/index.tsx"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LoginIndex />
  </React.StrictMode>
)
